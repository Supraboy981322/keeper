# libraries for random helpers for various (eventually) languages

>[!NOTE]
>Some of the functions are basically something that the languages'
>standard library does, but not in a way that makes sense to me
>(for example: Golang's `append(...)` builtin doesn't alter the original slice)
